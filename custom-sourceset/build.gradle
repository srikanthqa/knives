apply from: "$rootDir/gradle/javaProject.gradle"

dependencies {
	compile localGroovy()
	compile gradleApi()
}

group = 'com.github.knives.gradle'
version = '1.0.0-SNAPSHOT'

task integrationTest(type: GradleBuild) {
	buildFile = 'sourceset.gradle'
	tasks = ['printAllSource']
}