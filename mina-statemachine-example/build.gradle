apply from: "$rootDir/gradle/javaProject.gradle"
apply plugin: 'application'

dependencies {
	compile group: 'org.apache.mina', name: 'mina-statemachine', version: minaVersion
}

mainClassName = 'com.github.knives.mina.MinaStateMachine'
